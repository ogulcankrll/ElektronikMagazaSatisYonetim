@model EntityLayer.Models.Urun

@{
    ViewData["Title"] = "Ürün Ekle";
    Layout = "~/Views/Shared/_CoreLayout.cshtml";
}
<h2>Ürün Ekle</h2>
div class="container mt-3">
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h4>Yeni Ürün Ekle</h4>
            </div>
            <div class="card-body">
                <form asp-action="UrunEkle" method="post" enctype="multipart/form-data">
                    <div class="form-group mb-3">
                        <label>Ürün Adı</label>
                        <input asp-for="Ad" class="form-control" placeholder="Ürün adını giriniz" />
                    </div>

                    <div class="form-group mb-3">
                        <label>Kategori</label>
                        <select asp-for="KategoriID" class="form-control" asp-items="ViewBag.Kategoriler">
                            <option value="">Kategori Seçiniz</option>
                        </select>
                    </div>

                    <div class="form-group mb-3">
                        <label>Marka</label>
                        <select asp-for="MarkaID" class="form-control" asp-items="ViewBag.Markalar">
                            <option value="">Marka Seçiniz</option>
                        </select>
                    </div>

                    <div class="form-group mb-3">
                        <label>Fiyat</label>
                        <div class="input-group">
                            <input asp-for="Fiyat" class="form-control" placeholder="0.00" />
                            <div class="input-group-append">
                                <span class="input-group-text">₺</span>
                            </div>
                        </div>
                    </div>

                    <div class="form-group mb-3">
                        <label>Ürün Resmi</label>
                        <input type="file" name="resimDosyasi" class="form-control" />
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-save"></i> Kaydet
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="fa fa-list"></i> Listeye Dön
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-header bg-info text-white">
                <h4>Bilgi</h4>
            </div>
            <div class="card-body">
                <p>Bu sayfada yeni ürün ekleyebilirsiniz. Tüm alanları doldurmanız önerilir.</p>
                <hr />
                <p><strong>Resim URL:</strong> Resmin tam yolunu belirtmeniz gerekir.</p>
                <p><small>Örnek: /urunResimleri/urun1.jpg</small></p>
            </div>
        </div>
    </div>
</div>
</div>